#!/bin/sh
set -eu
# This tooling package does not install/own a live vendor service. Do not stop
# an operator-managed service merely because it has the same suggested name.
# Configuration, imported vendor releases, databases, logs, and backups are preserved.
exit 0
