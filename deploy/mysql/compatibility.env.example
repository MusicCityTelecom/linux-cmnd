# Compatibility-lab only. Pin images by digest before an approved deployment.
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3307
MYSQL_LOWER_CASE_TABLE_NAMES=0
# Read credentials from a mode-0600 secret file; never add them here.
